#include "main.h"

/**
 * print_line - this prints straight line `n` times
 * @n: the number of times the line will print
 * Return: none.
 */
void print_line(int n) 
 { 
         int i; 
  
         for (i = 0; i < n; i++) 
         { 
                 _putchar(95); 
         } 
         _putchar('\n'); 
 }
